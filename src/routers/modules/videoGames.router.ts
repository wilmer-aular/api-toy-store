import { Router } from 'express';
//import { all, find, filter, create, remove, update } from '/controllers/staff.controller';

const router = Router();

// router.get('/',  all);
// router.get('/filter',  filter);
// router.get('/:id',  find);
// router.post('/',  create);
// router.put('/:id',  update);
// router.delete('/:id',  remove);

export default router;